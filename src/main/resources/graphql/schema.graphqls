schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}

enum RoleName {
  USER
  ADMIN
}

enum StatusName {
  active
  closed
  pending
  deleted
}

enum Period {
  TODAY
  YESTERDAY
  DAY
  MONTH
  YEAR
  ALL
}

enum DateSort {
  CREATED_AT
  UPDATED_AT
}

enum DirectionSort {
  ASC
  DESC
}
